# Credentials — never commit these
.env
secrets.json
config.json

# Output files (keep the module code: output/__init__.py, output/markdown.py, output/epub.py)
output/*.md
output/*.epub
output/*/
test-output/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# macOS
.DS_Store

# Briefcase build artifacts
build/
dist/
logs/
*.iconset/

# Claude Code / Spectra (local dev config, not part of the project)
.claude/
.spectra/
openspec/
CLAUDE.md
.claude-memory
opencode.json
openspec/.vector-search.db*
